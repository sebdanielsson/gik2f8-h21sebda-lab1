{
  "devDependencies": {
    "tailwindcss": "3.2.2"
  },
  "scripts": {
    "tailwindcss": "npx tailwindcss -i ./src/input.css -o ./build/output.css",
    "build": "mkdir -p ./build; mv `ls -1 ./src/ | grep -v input.css` ./build"
  }
}
