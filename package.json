{
  "devDependencies": {
    "tailwindcss": "3.3.0"
  },
  "scripts": {
    "tailwindcss-dev": "npx tailwindcss -i ./src/input.css -o ./build/output.css --watch",
    "tailwindcss-build": "npx tailwindcss -i ./src/input.css -o ./build/output.css",
    "build": "mkdir -p ./build; ls -1 ./src/ | grep -v ^input.css | grep -v ^.DS_Store | xargs -I{} cp -r src/{} ./build;cp README.md ./build/README.md;"
  }
}
